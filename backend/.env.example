# Backend environment variables
# Copy this to .env and fill in values before deploying

# REQUIRED: Change this to a long random string in production!
SECRET_KEY=change-this-to-a-long-random-secret

# REQUIRED: Change the default admin password before deploying!
ADMIN_PASSWORD=change-this-password

# Optional: Use PostgreSQL in production (recommended for Azure/Render)
# DATABASE_URL=postgresql://user:password@host:5432/dbname
# If not set, SQLite is used (fine for testing, not recommended for production)

# Optional: Restrict CORS to your frontend domain
# CORS_ORIGIN=https://your-frontend-domain.com

# Port (set automatically by Render/Azure)
# PORT=5000
